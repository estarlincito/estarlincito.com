{
  "name": "@repo/constants",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "pnpm lint && tsc && tsc-alias && node dist/index.js",
    "lint": "eslint --fix && eslint --ext .js,.ts . && pnpm tsc --noEmit"
  },
  "dependencies": {
    "@repo/lib": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*"
  }
}
