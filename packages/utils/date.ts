// import type { Locale } from '@repo/content/shared/locales';

// export const getCurrentDate = (lng: Locale) =>
//   new Intl.DateTimeFormat(lng, { dateStyle: 'medium' }).format(new Date());

// const dateFormat = (lng: Locale) =>
//   new Intl.DateTimeFormat(lng, { dateStyle: 'medium' }).format(new Date());
